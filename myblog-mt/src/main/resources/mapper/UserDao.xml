<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.esieve.user.dao.UserDao">

    <select id="getUsers" resultType="User">
        SELECT * FROM user
    </select>

    <select id="getUserByUserId" resultType="User" parameterType="int">
        SELECT user_id,username,image FROM user
        WHERE user_id=#{userId};
    </select>

    <select id="getUserByUser" resultType="User" parameterType="User">
        SELECT user_id,username,image FROM user
        WHERE username=#{username} AND password=#{password};
    </select>

    <insert id="insertUser" parameterType="User">
        INSERT INTO user
        VALUES (NULL,#{username},#{password},#{image});
    </insert>

    <update id="updateUser" parameterType="User">
        UPDATE user SET username=#{username}, password=#{password}, image=#{image}
        WHERE user_id=#{userId}
    </update>

    <delete id="deleteUser" parameterType="int">
        DELETE FROM user
        WHERE user_id=#{userId}
    </delete>
</mapper>